/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.ibb.co', 'lh3.googleusercontent.com','s.gravatar.com', 'cdn.auth0.com', 'graph.facebook.com', 'api-microsoft.com'], 
      },
};

export default nextConfig;
